apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: studypoint-prod
resources:
  - ../../base
configMapGenerator:
  - name: studypoint-config
    behavior: merge
    literals:
      SPRING_PROFILES_ACTIVE: "prod"
      CORS_ALLOWED_ORIGINS: "https://studypoint.com"